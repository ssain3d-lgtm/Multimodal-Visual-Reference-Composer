{
  "name": "unified-visual-reference-composer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Search with words, images, videos or references; extract only the visual elements you want, mix them, and turn the result into a structured generation prompt.",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "python3 -m http.server 8765",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run check:taxonomy && npm run check:schemas && npm run check:docs && npm test",
    "check:taxonomy": "node tests/validate-taxonomy.mjs",
    "check:schemas": "node tests/validate-schemas.mjs",
    "check:docs": "node tests/validate-docs.mjs",
    "seed": "node tools/gen-seed.mjs"
  }
}
